## -*- truncate-lines: t; -*-
## Time-stamp: <2015-06-17 16:43:01 CEST (es)>

## nth <- function (x, n, first = 1L)
##     if (is.matrix(x))
##         x[seq(first, nrow(x), by = n), ] else
##             x[seq(first, length(x), by = n)]
