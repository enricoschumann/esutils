## -*- truncate-lines: t; fill-column: 65; comment-column: 50; -*-
## Time-stamp: <2015-06-17 16:44:05 CEST (es)>


## xylabels <- function(x,y,labels, ...) {
##     d <- par()$usr
##     xx <- (d[2] - d[1]) * as.numeric(x) + d[1]
##     yy <- (d[4] - d[3]) * as.numeric(y) + d[3]
    
##     text(x = xx, y = yy, labels = labels, ...)
##     invisible(c(xx,yy))
## }
